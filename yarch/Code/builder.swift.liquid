//
//  {{ module_info.description }}
//  Created by {{ developer.name }} on {{ date }}.
//

import UIKit

class {{ module_info.name }}Builder: ModuleBuilder {

    var initialState: {{ module_info.name }}.ViewControllerState?

    func set(initialState: {{ module_info.name }}.ViewControllerState) -> {{ module_info.name }}Builder {
        self.initialState = initialState
        return self
    } 

    func build() -> UIViewController {
        let presenter = {{ module_info.name }}Presenter()
        let interactor = {{ module_info.name }}Interactor(presenter: presenter)
        let controller = {{ module_info.name }}ViewController(interactor: interactor)

        presenter.viewController = controller
        return controller
    }
}
