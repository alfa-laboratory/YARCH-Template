//
//  Created by {{ developer.name }} on {{ date }}.
//

protocol {{ module_info.name }}ServiceProtocol {
    func fetchItems(completion: @escaping ([{{ module_info.name }}Model]?, Error?) -> Void)
}

/// Получает данные для модуля {{ module_info.name }}
class {{ module_info.name }}Service: {{ module_info.name }}ServiceProtocol {

    func fetchItems(completion: @escaping ([{{ module_info.name }}Model]?, Error?) -> Void) {
        completion(nil, nil)
    }
}
