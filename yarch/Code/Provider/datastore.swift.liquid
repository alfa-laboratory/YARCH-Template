//
//  Created by {{ developer.name }} on {{ date }}.
//

/// Класс для хранения данных модуля {{ module_info.name }}
class {{ module_info.name }}DataStore {
    var models: [{{ module_info.name }}Model]?
}
